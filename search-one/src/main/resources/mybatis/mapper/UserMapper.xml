<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cap.mapper.UserMapper">
    <select id="queryUser" resultType="User">
        select * from search.user where name = #{name}
    </select>
    <insert id="addUser">
        insert into user(name,password) values (#{name},#{password})
    </insert>
    <insert id="addRecord">
        insert into record(userid,dataid,recordtime) values (#{userId},#{dataId},#{recordTime})
    </insert>
    <update id="updateRecord">
        update record set recordtime = #{recordTime} where userid = #{userId} and dataid = #{dataId}
    </update>
    <select id="queryRecord" resultType="Record">
        select * from record where userid = #{userId} and dataid = #{dataId}
    </select>
    <select id="queryRecordByUserId" resultType="java.lang.String">
        select dataid from record where userid = #{userId} order by recordtime desc
    </select>
    <select id="queryCollection" resultType="CollectionData">
        select * from collection where userid = #{userId} and dataid = #{dataId}
    </select>
    <select id="queryCollectionByUserId" resultType="java.lang.String">
        select dataid from collection where userid = #{userId} order by collectiontime desc
    </select>
    <insert id="addCollection">
        insert into collection (userid, dataid, collectiontime) values (#{userId},#{dataId},#{collectionTime})
    </insert>
    <delete id="deleteCollection">
        delete from collection where dataid = #{dataId} and userid = #{userId}
    </delete>
</mapper>